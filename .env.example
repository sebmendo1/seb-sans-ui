# Client-side admin dashboard PIN (embedded at build time for static deploy).
VITE_ADMIN_PIN=sebsans

# Pull production submissions into public/data/submissions.json:
# npm run seed:pull

# Local full-stack dev only (npm run dev):
# ADMIN_PIN=sebsans
# DATABASE_URL=sqlite:///./data/survey.sqlite3
