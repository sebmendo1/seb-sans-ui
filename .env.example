ADMIN_PIN=sebsans
DATABASE_URL=sqlite:///./data/survey.sqlite3
SESSION_COOKIE_SECURE=false
# Comma-separated browser origins allowed to call the API (for split frontend/API deploys).
# ALLOWED_ORIGINS=https://your-frontend.example.com
# When the API is on a different host than the Vite build, set this at build time:
# VITE_API_BASE_URL=https://your-api.example.com
